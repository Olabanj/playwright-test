node_modules/
# Run-scoped login cache written by global-setup.ts (machine-local, never committed).
.auth/
# Claude Code project tooling — .claude/agents/, skills/, README.md are tracked
# so the team inherits the agentic migration system on clone. Only personal
# permissions / MCP config stays local.
.claude/settings.local.json
.env
.env.local
.env.staging
.env.prod
test-results/
playwright-report/
playwright/.cache/
# Playwright codegen / --ui artifacts — not part of our workflow (see docs/20-engineering/local-setup.md)
specs/
**/seed.spec.ts
.playwright-mcp/
*.log
.DS_Store
# Python virtualenv for the migration dashboard (machine-specific paths).
# The dashboard itself lives in docs/test-migration/dashboard/ and IS committed.
docs/test-migration/dashboard/.venv/
# Generated / machine-local artifacts — never tracked here.
# (The Graphify graph in graphify-out/ is regenerated by the post-commit hook.)
graphify-out/
docs/.obsidian/
*.zip
# PEM certificates / private keys (e.g. sandbox_rp.pem) — never commit.
*.pem
.claude/worktrees/

# Machine-local test-run artifacts (token caches, storage state, seeded prerequisites)
fixtures/data/
